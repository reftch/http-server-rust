[package]
name = "server"
version = "0.1.0"
edition = "2024"

[dependencies]
request = { path = "../request" }
response = { path = "../response" }
router = { path = "../router" }
libc = "0.2"

[profile.release]
lto = true
codegen-units = 1
opt-level = 3
