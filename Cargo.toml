[package]
name = "web-server"
version = "0.1.0"
edition = "2024"

[dependencies]
mio = { version = "1", features = ["net", "os-poll", "os-ext"] }
slab = "0.4"

[profile.release]
lto = true
codegen-units = 1
opt-level = 3